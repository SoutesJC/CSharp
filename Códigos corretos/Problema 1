/*
Console.WriteLine("Digite seu nome completo: ");
string primeiroNome = Console.ReadLine();
*/
// Erro 01 : Conversão de Literal nula ou possivel valor nulo em tipo não anulável.
// CS8600
// A função Console.ReadLine() retorna valores inclusives nulos, porém o tipo atribuído "string" não aceita valores nulos, para resolver podemos apenas colocar o digito "!" para converter null em uma string.
//Correção:
Console.WriteLine("Digite seu primeiro nome: ");
string primeiroNome = Console.ReadLine()!;

/*
Console.WriteLine("Agora, digite sua idade: ");
int idade = Console.ReadLine();
*/
//Erro 02 :Não é possível converter implicitamente tipo "string" em "int".
//: A função Console.ReadLine retorna uma string, porém a variável atribuída é do tipo inteira. Erro de tipagem básico.
//Correção: 
Console.WriteLine("Agora, digite sua idade: ");
int idade = /*Convert.ToInt32*/int.Parse(Console.ReadLine()!);

/*
Console.WriteLine("Por fim, digite sua altura em metros (ex: 1,80): ");
float altura = float.Parse(Console.ReadLine());
*/
//Erro 03: Possível argumento de referência nula para o parâmetro 's' em 'float float.Parse(string s)'.
//A função Console.ReadLine() pode retornar um valor nulo, porém a atribuição float altura não aceita valores nulos como entrada.

//Correção:
Console.WriteLine("Por fim, digite sua altura em metros (ex: 1,80): ");
float altura = float.Parse(Console.ReadLine()!);

/*
Console.WriteLine($"Resumo do Cadastro: Nome: {primeiroNome}, Idade: {idade} anos, Altura: {altura}m.")
*/
//Erro 04: ; esperado.
// Erro de sintaxe comum. Apenas o caracter ";" não foi adicionado no final da expressão.

//Correção:
Console.WriteLine($"Resumo do Cadastro: Nome: {primeiroNome}, Idade: {idade} anos, Altura: {altura}m.");
